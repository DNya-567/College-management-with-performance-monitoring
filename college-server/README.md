# College Server

Minimal notes for local development.

## Attendance

- Run the SQL migration in `sql/2026_02_15_create_attendance_table.sql`.
- Start the server and hit endpoints under `/api/classes/:classId/attendance`.

## Smoke Check

```powershell
node scripts/attendance-smoke.js
```
